sudo: required

branches:
  only:
  - master
  - development

matrix:
  include:
    - language: python
      python: 3.6
      cache:
        pip3: true
      install:
        - pip3 install django coverage djangorestframework Pillow
      before_script:
        - cd backend
        - python3 manage.py migrate
      script:
        - coverage run --branch --source="./plain_plane" manage.py test
      after_script:
        - cd ..

    - language: node_js
      node_js: 8.8
      addons:
        apt:
          sources:
            - google-chrome
          packages:
            - google-chrome-stable
      cache:
        npm: true
        directories:
          - frontend/node_modules
      before_install:
        - export CHROME_BIN=/usr/bin/google-chrome
        - export DISPLAY=:99.0
        - sh -e /etc/init.d/xvfb start
        - cd frontend
      install:
        - npm install
        - npm install coveralls
        - npm install -g @angular/cli
      script:
        - ng test --code-coverage --watch false
        - ng e2e
      after_script:
        - cd ..
